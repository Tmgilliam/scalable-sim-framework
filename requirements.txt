# Core scientific stack
numpy>=1.26,<3.0
pandas>=2.1,<3.0

# Config / validation
pydantic>=2.5,<3.0
PyYAML>=6.0,<7.0

# Dashboard
streamlit>=1.32,<2.0
plotly>=5.18,<6.0

# Vertex AI / Kubeflow Pipelines component authoring
# kfp is optional at runtime; pipeline.py degrades to a local executor
# when kfp is not installed.
kfp>=2.7,<3.0

# Testing / linting
pytest>=8.0,<9.0
pytest-cov>=4.1,<6.0
flake8>=7.0,<8.0
